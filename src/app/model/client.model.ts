export class Client {

    id: string;

    constructor( id: string ) {
        this.id = id;
    }



}


// Object {subscription: "none", ask: "subscribe"} - client that sent request
// Object {subscription: "none", ask: null} - client that got request
